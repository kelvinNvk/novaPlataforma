import "express";

declare global {
  namespace Express {
    interface UserPayload {
      id: number;
      role: string;
    }

    interface Request {
      user?: UserPayload; // agora o req.user existe de forma tipada
    }
  }
}
